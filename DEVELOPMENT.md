# SKYBREAK 開発方針 / Source of Truth

Status: Combat Foundation / Prototype
Category: ACTION
Target: Smartphone landscape first

## 1. 目的

SKYBREAK は、スマートフォン横画面で遊ぶことを主目的とした、オリジナルのプラットフォームファイターです。

開発者が内容を把握していても完成後に繰り返し遊べるゲームを目指し、ストーリー消費型ではなく、対戦・操作・キャラクター追加・ステージ追加によって継続的に遊べる構造を採用します。

## 2. リポジトリ構成方針

ゲーム本体は `KDK941007/skybreak` で管理します。

ポータルサイト `KDK941007/KDKGames` にはゲーム本体の画像・音源・大容量素材を置きません。ポータル側には軽量なランチャーだけを配置し、そこから SKYBREAK の GitHub Pages を開きます。

理由:

- キャラクター画像、エフェクト、ステージ、BGM等で容量が大きくなるため
- ポータル側のGit容量を増大させないため
- ポータル側Service Workerのオフラインキャッシュを肥大化させないため
- SKYBREAK単体で素材・機能・公開方式を独立して拡張できるようにするため

ポータル上のカテゴリは **ACTION** とします。

## 3. 最重要開発方針

### 3.1 描画品質を優先する

キャラクター・ステージ・主要エフェクトは画像素材をベースに実装します。

キャラクターを単純なCanvas図形で代替しません。容量削減のために描画品質を大幅に落とすこともしません。

### 3.2 攻撃・被弾時の見切れを許容しない

過去試作では、攻撃時・被弾時にエフェクトやキャラクターの一部が見切れる問題が繰り返し発生しました。

この問題をサイズ縮小だけで誤魔化してはいけません。

現在の基盤では以下を原則とします。

- キャラクターと攻撃エフェクトを別レイヤーで管理する
- 攻撃側の斬撃エフェクトは原則キャラクター背面に配置する
- 被弾・ヒットスパークは前面レイヤーに配置する
- エフェクト座標はpx基準で算出する
- 画面端ではエフェクト中心を安全領域へ補正する
- キャラクター表示ボックス自体に十分な余白を持たせる
- 元画像にも透明余白を確保する
- `overflow:hidden` に依存した切り捨てで見た目を成立させない

### 3.3 動きの滑らかさを優先する

現在の戦闘基盤では次を採用します。

- `requestAnimationFrame` による描画
- 120Hz相当の固定タイムステップで物理更新
- 描画時に前フレームと現在フレームを補間する
- キャラクター位置更新は `translate3d` を利用する
- フレーム落ち時も一度に巨大な移動をしない

画像アニメーション自体のフレーム数が不足する場合は、実装上の補間だけで誤魔化さず、必要に応じてスプライト素材を追加します。

## 4. 現在の操作仕様

- 左スティック: 左右移動
- JUMP: ジャンプ
- 空中でJUMP: 2段ジャンプ
- A: 通常攻撃
- B: 必殺弾
- PORTAL: KDKGamesポータルへ戻る

ジャンプは「低すぎず、高すぎない」ことを重視し、現在は1段目と2段目で初速を分けています。

## 5. 現在の開発フェーズ

いきなりキャラクター・ステージ・技を大量追加しません。

まず以下の戦闘基盤を完成させます。

1. 移動が滑らかである
2. 1段ジャンプが自然な高さである
3. 2段ジャンプが自然に連続する
4. 通常攻撃でキャラクターもエフェクトも見切れない
5. 必殺技でキャラクターもエフェクトも見切れない
6. 被弾時にキャラクターもヒットエフェクトも見切れない
7. スマートフォン横画面でUIが操作を阻害しない

この基盤がユーザーレビューでOKになるまで、大規模なコンテンツ追加より基礎品質を優先します。

## 6. 完成版に向けた想定機能

戦闘基盤承認後に順次検討します。

- プレイヤーキャラクター追加
- キャラクターごとの通常技・必殺技差別化
- CPU対戦
- 複数ストック制
- ダメージ蓄積と吹っ飛び
- ガード / 回避
- 複数ステージ
- キャラクター選択
- リザルト
- BGM / 効果音
- ゲームバランス調整
- 必要に応じたオフライン対応

機能を追加する場合も、操作感・描画品質を壊さないことを優先します。

## 7. 素材方針

- オリジナル素材を使用する
- 既存ゲームのキャラクター画像・ステージ・効果音等をそのまま流用しない
- キャラクター素材は用途別に十分な余白を確保する
- エフェクト素材は攻撃 / 被弾 / 必殺等で用途を分離する
- 未使用素材は無理にリポジトリへ入れない

## 8. ディレクトリ方針

現在:

```text
skybreak/
├─ index.html
├─ style.css
├─ game.js
├─ assets/
│  ├─ stage.webp
│  ├─ rex_*.webp
│  ├─ aion_*.webp
│  └─ fx_*.webp
├─ README.md
├─ DEVELOPMENT.md
└─ .nojekyll
```

正式開発で規模が増えた場合は、以下の分割を検討します。

```text
assets/
├─ characters/
├─ effects/
├─ stages/
└─ audio/
js/
├─ core/
├─ battle/
├─ ui/
└─ data/
css/
```

分割は「必要になってから」行い、初期段階で過度に複雑化しません。

## 9. Git運用

- `main` は動作確認済みの基準版とする
- 原則 `feature/*` ブランチで変更する
- 大きな変更はPRで差分確認後に `main` へ反映する
- 画像素材もゲーム挙動と同じコミット / PRで追跡可能にする
- ポータル側リポジトリとは独立してリリースできるようにする

## 10. GitHub Pages / ポータル連携

SKYBREAK公開先の想定URL:

`https://kdk941007.github.io/skybreak/`

KDKGames側は `games/action/skybreak/` を playable なゲームフォルダとして持ち、ポータル規約どおり `gameTile available` からこのローカルフォルダへ遷移します。

ランチャーが SKYBREAK のGitHub Pagesへ転送します。

SKYBREAK側の `PORTAL` ボタンは次へ戻します。

`https://kdk941007.github.io/KDKGames/`

## 11. 現在の完了条件

現在フェーズの完了条件は以下です。

- スマホ横画面でプレイ可能
- 1段 / 2段ジャンプの高さがレビューで妥当
- 移動が十分滑らか
- 通常攻撃時に見切れなし
- 必殺技時に見切れなし
- 被弾時に見切れなし
- キャラクター本体がエフェクトで不自然に隠れない
- PORTALからKDKGamesへ戻れる

この条件を満たしてから、正式な対戦ゲーム機能の拡張へ進みます。
