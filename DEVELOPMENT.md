# SKYBREAK 開発方針 / Source of Truth

Status: Combat Foundation / Prototype
Category: ACTION
Target: Smartphone landscape first

## 1. 目的

SKYBREAK は、スマートフォン横画面で遊ぶことを主目的としたオリジナルのプラットフォームファイターです。

完成後も繰り返し遊べることを重視し、対戦・操作・キャラクター追加・ステージ追加によって継続的に遊べる構造を目指します。

## 2. リポジトリ構成方針

ゲーム本体は `KDK941007/skybreak` で管理します。

ポータルサイト `KDK941007/KDKGames` には本体の画像・音源・大容量素材を置かず、軽量ランチャーのみ配置します。ランチャーから SKYBREAK の GitHub Pages を開きます。

理由:

- キャラクター画像、エフェクト、ステージ、BGM等で容量が大きくなるため
- ポータル側のGit容量を増大させないため
- ポータル側Service Workerのオフラインキャッシュを肥大化させないため
- SKYBREAK単体で素材・機能・公開方式を独立して拡張できるようにするため

ポータル上のカテゴリは **ACTION** とします。

## 3. 最重要開発方針

### 3.1 描画品質を優先する

キャラクター・ステージ・主要エフェクトは画像素材をベースに実装します。

キャラクターを単純なCanvas図形で代替しません。容量削減を理由に描画品質を大幅に落としません。

### 3.2 攻撃・被弾時の見切れを許容しない

攻撃時・被弾時にエフェクトやキャラクターの一部が見切れる問題を許容しません。サイズ縮小だけで誤魔化さず、素材側と描画側の両方を確認します。

原則:

- キャラクター本体と攻撃エフェクトを別レイヤーで管理する
- 攻撃側の斬撃エフェクトは原則キャラクター背面に配置する
- 被弾・ヒットスパークは前面レイヤーに配置する
- エフェクト座標はpx基準で算出する
- 画面端では安全領域を考慮する
- キャラクター表示領域と元画像の両方に十分な余白を確保する
- 余計なセル断片・手足・別ポーズが混入したスプライトを使用しない
- `overflow:hidden` に依存した切り捨てで見た目を成立させない

### 3.3 動きの滑らかさを優先する

現在の戦闘基盤では以下を採用します。

- `requestAnimationFrame` による描画
- 120Hz相当の固定タイムステップで物理更新
- 描画時に前フレームと現在フレームを補間する
- キャラクター位置更新は `translate3d` を利用する
- フレーム落ち時も一度に巨大な移動をしない

画像アニメーション自体のフレーム数が不足する場合は、実装上の補間だけで誤魔化さず必要なスプライト素材を追加します。

## 4. 現在の操作仕様

- 左スティック左右: 左右移動
- 左スティック下: しゃがみ
- JUMP: 1段ジャンプ
- 空中でJUMP: 2段ジャンプ
- A: 通常攻撃
- B: 必殺弾
- GUARD: 押している間ガード。移動入力・ジャンプで解除
- PORTAL: KDKGamesポータルへ戻る

ジャンプはレビューを基準に高さを調整します。

## 5. 現在の戦闘基盤

現在は以下の構成を基準とします。

- ステージ横幅は **4画面分**。直前の2画面版からさらに2倍に拡張
- プレイヤー移動に合わせてカメラ追従
- キャラクターの足元は論理GROUNDへ直接アンカーし、数pxだけ床側へ沈めて接地感を出す
- キャラクター直下に接地影を表示し、空中では影を縮小・薄くする
- 着地時に軽い着地エフェクトを表示する
- 1段 / 2段ジャンプ対応
- スティック下入力によるしゃがみ対応。足元位置を固定したまま低姿勢化する
- GUARDボタンによるガード表示・操作を実装
- 通常攻撃はクリーンな既存ポーズを複数フレーム切替し、踏み込み・回転・斬撃VFXを同期して攻撃アニメーションを構成する
- キャラクター本体と斬撃VFXを分離し、断片混入のある旧attackスプライトは使用しない
- クリーンアップ済み透過PNGをキャラクター素材に使用する
- ステージは `stage.webp` を使用する

## 6. 現在フェーズの完了条件

以下を満たすまで、大規模なコンテンツ追加より戦闘基盤の品質を優先します。

1. スマホ横画面でプレイ可能
2. キャラクターが床面に自然に接地して見える
3. 4画面分のステージを移動でき、カメラ移動が自然
4. 1段ジャンプが自然な高さ
5. 2段ジャンプが自然に連続
6. スティック下で自然にしゃがめる
7. GUARDが操作可能で表示も分かりやすい
8. 移動が十分滑らか
9. 通常攻撃でキャラクター本体も明確にアニメーションする
10. 通常攻撃時にキャラクター・エフェクトの見切れなし
11. 必殺技時にキャラクター・エフェクトの見切れなし
12. 被弾時にキャラクター・エフェクトの見切れなし
13. UIが操作を阻害しない
14. PORTALからKDKGamesへ戻れる

## 7. 完成版に向けた想定機能

戦闘基盤承認後に順次検討します。

- プレイヤーキャラクター追加
- キャラクターごとの通常技・必殺技差別化
- CPU対戦
- 複数ストック制
- ダメージ蓄積と吹っ飛び
- 回避
- 複数ステージ
- キャラクター選択
- リザルト
- BGM / 効果音
- ゲームバランス調整
- 必要に応じたオフライン対応

## 8. 素材方針

- オリジナル素材を使用する
- 既存ゲームのキャラクター画像・ステージ・効果音等をそのまま流用しない
- キャラクター素材は用途別に十分な透明余白を確保する
- エフェクト素材は攻撃 / 被弾 / 必殺等で用途を分離する
- スプライトシートから切り出す場合、隣接セル断片が混入していないことを確認する
- 未使用素材は無理にリポジトリへ入れない

## 9. ディレクトリ方針

現在:

```text
skybreak/
├─ index.html
├─ style.css
├─ game.js
├─ assets/
│  ├─ stage.webp
│  ├─ rex_*.png
│  ├─ aion_*.png
│  └─ fx_*.png
├─ README.md
├─ DEVELOPMENT.md
├─ ASSET_MANIFEST.md
└─ .nojekyll
```

規模が増えた場合のみ、`assets/characters/`、`assets/effects/`、`assets/stages/`、`assets/audio/`、`js/`、`css/` への分割を検討します。

## 10. Git運用

SKYBREAKでは、今後の開発速度を優先し **原則 `main` へ直接修正・コミットする** 方針とします。

- 通常のゲーム修正は `main` に直接反映する
- 画像素材も `main` へ直接コミットしてよい
- `feature/*` ブランチやPRは必須としない
- ユーザーが明示的にブランチ運用を指定した場合、または大規模・高リスク変更で一時分離が必要な場合のみfeatureブランチを使用する
- `main` へ反映する前に、可能な範囲で構文・参照パス・既存仕様への影響を確認する
- ポータル側 `KDKGames` のGit運用はSKYBREAKとは別管理とし、ポータル固有ルールを優先する

## 11. GitHub Pages / ポータル連携

SKYBREAK公開先:

`https://kdk941007.github.io/skybreak/`

KDKGames側は `games/action/skybreak/` をplayableなゲームフォルダとして持ち、ポータル規約どおり `gameTile available` からこのローカルフォルダへ遷移します。

ランチャーから SKYBREAK のGitHub Pagesへ転送します。

SKYBREAK側の `PORTAL` ボタンは以下へ戻します。

`https://kdk941007.github.io/KDKGames/`

## 12. 画像投入について

修正版画像資材はリポジトリ直下の `assets/` へ配置します。

`assets/assets/...` の二重階層にしないでください。

画像資材が揃ったmain push後、GitHub Pages Workflowが必要ファイルを確認して公開処理を実行します。
